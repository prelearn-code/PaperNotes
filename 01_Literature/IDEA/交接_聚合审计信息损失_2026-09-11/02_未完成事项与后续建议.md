# 未完成事项与后续建议

## 1. 未完成清单（按优先级）

### P0 — 论文主体，必须做

| # | 事项 | 为什么必须 | 验收标准 | 预估 |
|---|---|---|---|---|
| P0-1 | **判据定理化**：把 `判定规则_v1` 的 R1–R7 写成对**受限审计表达式语言**的判定规则，并证明"判为失效 ⇒ 存在多项式时间的充分状态应答算法"（健全性），同时给"证据不足"一个明确语义 | 这是贡献一的核心。没有它，判据只是检查表，审稿人会问"凭什么这三条规则够用" | ① 表达式语言的语法与语义；② 每条规则的判定条件；③ 健全性定理与证明；④ "证据不足"的输出语义；⑤ 用现有 7 个案例逐条验证（Song/Zhang 判失效、Yang/Miao/基线判不失效、OTGPDP/Li Hang 正确分级） | 中高（纯理论） |
| P0-2 | **判据工具化**：接受**声明式输入**（状态拆解、字段数据依赖、验证式、威胁模型原句），输出证书 | 贡献三按新结构就是它；当前 7 个案例是 7 份手写模块，不是工具 | ① 定义输入格式（建议 JSON/YAML）；② 规则引擎实现 R1–R7；③ 输出与现证书同 schema；④ 用 7 个案例做回归（结果必须与手写模块一致）；⑤ 支持"证据不足"分支 | 中 |
| P0-3 | **贡献三定位裁决**：写一页"什么才算新机制"的判据并据此决定是否继续 | 决定论文是"分析框架"还是"新原语"，影响全部后续投入 | 判据须同时要求：(a) 保持原聚合效率（一周期一证明覆盖多文件或统一搜索+审计）；(b) 可证提取性；(c) 明显优于"独立底数+逐 sector 响应+挑战绑定"的平凡组合；**(d) 优于 Yang 2026 / Miao 2025 已有的构造**。若无 (c)/(d) 则**不做实现、不列为新贡献** | 低（半天，纸面） |

### P1 — 加固与一致性

| # | 事项 | 说明 |
|---|---|---|
| P1-1 | **第三方复核验证方程** | 现有"独立第二读法"由同一人完成，仍可能共享系统性误读；最低成本是请他人按 PDF 重读 Zhang Eq. (7)(8) 与 Song Eq. (9)(10) |
| P1-2 | B8/B9 逐字复核 | Song 的周期语义（已搜索/未搜索）、Zhang 上传校验里 `π1`/`π2` 的配对读法；两者目前按字面实现并自洽，但未与原文再逐字比对 |
| P1-3 | 认证基线在大参数下复核 | `results/baseline/*` 仍是缩减参数；为与 B5 一致，可在 256 位参数下重跑基线关键路径 |
| P1-4 | 把 Yang/Miao 的"权重可计算性"对照写进判据文档与论文 | 已进 R1，但需要与 P0-1 的表达式语言整合 |
| P1-5 | 派生文档存放规范 | **本次已踩坑**：`extractability-argument.md` 原先放在运行目录内，重跑时被清空（证书校验器当场报错）。规范：派生分析文档放 `evidence/<案例>/`，运行目录只放该次运行的原始产物 |

### P2 — 依赖外部材料，暂不可做

| # | 事项 | 缺什么 |
|---|---|---|
| P2-1 | Song 实际 `s` 与 sector 编码 | 作者代码/补充材料；影响"投影攻击是否适用于作者的实际参数" |
| P2-2 | Zhang 是否有作者实现（是否检查下标集） | 作者代码/更完整版本；若有检查，**单块 O(1) 攻击不成立** |
| P2-3 | Cheeturi 综述点名的 CLC 系列（Basha[38]、Sasikala[39][40]、Zhang[41]、Li[42]、Cheeturi[43]） | 原文不在本地语料；只在做"跨机制第三例"时才需要 |
| P2-4 | Zhao et al.（被 Zhu 2026 指为存在同类缺陷） | 同上 |

### 明确不做（已冻结）

- **不再扩充攻击案例**：停止条件已写在 `判定规则_v1` §4；两个真实正确方案的负面对照已完成。
- **Li Hang 不再扩展**；**OTGPDP 只在能解决原文歧义时继续**（本地做不到：初值不在原文，已按两难结论定稿）。
- **不做新审计机制实现**，除非 P0-3 的裁决通过。

## 2. 建议的论文结构

| 位置 | 内容 | 现有支撑 |
|---|---|---|
| 贡献一 | **充分状态分析框架与判定证书**：受限表达式语言、判定规则、健全性、可机器校验证书 | `判定规则_v1`、7 案例证书、独立复核、参数尺度复核 |
| 贡献二 | **两例原模型内攻击**（Song 2025、Zhang 2025，同属 IEEE TC）+ 两例边界案例（OTGPDP 显式变体、Li Hang 模型错位） | 各案例 L2 运行记录与协议映射 |
| 贡献三 | **分析工具与真实方案评估**（不强求新协议） | 待 P0-2；评估集已有 8 个方案（4 失效 + 3 不失效 + 1 正面对照） |
| 未来工作 | 若找到"保持原聚合效率且可证提取"的新结构，再恢复"新审计机制" | 待 P0-3 |

## 3. 建议的执行顺序

1. **P0-3（半天）**：先做裁决。若结论是"转工具"，则贡献三的定位立即确定，避免在构造上无限投入。
2. **P0-2（工具化）**：它会把判据的边界逼清楚（哪一步必须人工、哪一步可自动），反过来为 P0-1 的定理化提供精确的问题陈述；同时立刻可用 7 案例做回归。
3. **P0-1（定理化）**：在工具化的边界上写语言、规则与健全性证明。
4. P1 各项在 1–3 之间穿插（P1-1 可立即并行，成本最低）。

> 上一轮的判断：**先做工具化（P0-2）再做定理化（P0-1）**——工具化能立刻复用现有 7 案例，而定理化需要一个精确的问题陈述。

## 4. 交接时容易遗漏的三件事

1. **贡献二的"同机制"弱点是已知且已缓解**：两例原模型内攻击在内容侧同属机制类 1（固定投影），但 Zhang 另含**机制类 4（验证式不绑定挑战）**——这是独立缺陷，写论文时要显式区分。
2. **新颖性约束要写进论文**：Zhu 2026 已公开该攻击类别，Yang/Miao 已用"保密 sector 权重"的修复；创新只能落在统一判据、证书、跨方案实例与模型边界分级。
3. **状态压缩数字必须带口径**：编码（规范二进制/JSON）、群点（压缩/未压缩）、是否计入标签。四案例中只有 OTGPDP 的标签状态确实更大。
