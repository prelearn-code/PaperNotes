# ROUND1
## 方案修改1
1. 对于认证数据块，需要的参数信息是不是过多了，其中系统范围摘要、拥有权周期摘要还有version是不是都可以考虑一下必要性。还有段内编码、段标识。也可以考虑一下存在的必要性，不要为了复杂而复杂的设计结构。
2. 状态签名阶段，为什么还需要这么多的参数。首先解释一下方案的参数为什么那么多，以及每个参数的作用以及意义以及存在的必要性。
3. 关键词状态上下文也那么多的信息，是不是冗余信息过多了。
4. 更新文件块的信息，对于本方案设计的分段纠删码，是否需要更新的信息更多，更加复杂。还是说，本方案本来就不需要可实现块级别的更新的方案。
5. 段与版本根相关的部分，有活动段向量与本版向量这两个部分，

## 方案询问
1. 什么是双分量证书
2. 纠删编码是什么？
3. 签名授权有什么作用
4. 当前认证打开材料是什么
5. 认证树是什么结构
6. 打开的完整活动集合这个怎么获得，怎么保存，怎么使用
7. provider是代表数据拥有者还是CSP服务商。
8. 详细解释一下状态签名的各个参数的作用。
9. 都有什么时期，每个时期变化都需要这样的状态与时期记录吗
10. 底层的加密索引方法，看一下，只有一个简单的状态填充，是否还有具体化的实例。
11. 

# Round2
## 方案修改
1. 系统预控这个范围不需要，就在固定的域中使用，不需要单独描述。

## 方案询问

1. `OwnerSig`和`ProviderSig`具体使用的算法是什么？
2. `pos`怎么标识。
3. $u_i=H(\textsf{FASTIO-Addr}\parallel t_w\parallel st_w\parallel i)$里面涉及到的参数，`FASTIO-Addr`是什么意思。
4. 
